<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Nombre" %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label "Primer Apellido" %>
    <%= form.text_field :primerApellido %>
  </div>

  <div class="field">
    <%= form.label "Segundo Apellido" %>
    <%= form.text_field :segundoApellido %>
  </div>

  <div class="field">
    <%= form.label "Puntos Acumulados" %>
    <%= form.number_field :puntosAcumulados %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar" %>
  </div>
<% end %>
