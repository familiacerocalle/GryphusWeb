<%= form_with(model: challenge, local: true) do |form| %>
  <% if challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge.errors.count, "error") %> No se puede guardar el reto:</h2>

      <ul>
      <% challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Nivel Reto" %>
    <%= form.select :challengelevel_id, Challengelevel.all.map{|c| [c.descripcion, c.id]} %>
    <!--
    < %= form.text_field :challengelevel_id %>
    -->
  </div>

  <div class="field">
    <%= form.label "Descripción" %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar" %>
  </div>
<% end %>
