</br>
</br>
</br>
<div class="card">
  <div class="container-fliud">
    <div class="wrapper row">
      <div class="preview col-md-6">

        <div class="preview-pic tab-content">
          <% @complaint.attachments.each_with_index do |attachment, index| %>
            <div class="tab-pane <%= index == 0 ? 'active' : '' %>" id="pic-<%=attachment.id%>">
              <%= image_tag(attachment.file.url) %>
            </div>
          <% end %>
        </div>

        <ul class="preview-thumbnail nav nav-tabs">
          <% @complaint.attachments.each_with_index do |attachment, index| %>
            <li class="<%= index == 0 ? 'active' : '' %>">
              <a data-target="#pic-<%= attachment.id %>" data-toggle="tab">
                <%= image_tag(attachment.file.url) %>
              </a>
            </li>
          <% end %>
        </ul>

      </div>
      <div class="details col-md-6">
        <h6 class="sizes"><%= t(:description) %>: <%= @complaint.descripcion %></h6>
        <h6 class="sizes"><%= t(:complaint_type) %>: <%= @complaint.complaint_type.name %></h6>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-8">
    <h2 class="page-header"><%= t(:complaint_files) %></h2>
      <section class="comment-list">
        <% @complaint.complaintfiles.each do |file| %>
          <article class="row">
            <div class="col-md-2 col-sm-2 hidden-xs">
              <figure class="thumbnail">
                <%= image_tag file.archivo.url, class: "img-responsive" %>
                <figcaption class="text-center"><%= file.user.nombre %></figcaption>
              </figure>
            </div>
            <div class="col-md-10 col-sm-10">
              <div class="panel panel-default">
                <div class="panel-body">
                  <div class="comment-post">
                    <p><%= file.descripcion %></p>
                  </div>
                  <%= link_to t(:edit), edit_complaintfile_path(file) %>
                  <%= link_to t(:delete), file, method: :delete, data: { confirm: t(:are_you_sure) } %>
                </div>
              </div>
            </div>
          </article>
        <% end %>
      </section>
  </div>
</div>

<%= link_to t(:add_complaint_file), new_complaintfile_path(:complaint_id => @complaint.id) %>
<%= link_to t(:back), root_path %>