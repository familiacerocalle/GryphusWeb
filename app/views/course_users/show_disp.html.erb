<h1>Cursos Disponibles</h1>
<br>
<br>


<table>
  <tr>
    <th>CursoId</th>
    <th>CursoNombre</th>
    <th></th>
  </tr>

  <% @coursesdisp.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= course.nombre %></td>
      <td><%= link_to "Inscribir", {:controller => "course_users", :action => "inscribir", :courseid => course.id, :userid => current_user.id} %></td>
    </tr>
  <% end %>

</table>


<br>
<br>
<br>
<br>
<%= link_to 'Regresar', course_users_index_path %>
