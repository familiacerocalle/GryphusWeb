<h1>Cursos Disponibles</h1>
<br>
<br>


<table>
  <tr>
    <th>CursoId</th>
    <th>CursoNombre</th>
    <th></th>
  </tr>

  <% @coursesdisp.each do |course| %>
    <tr>
      <td><%= course.id %></td>
      <td><%= link_to course.nombre, course_path(course) %></td>
      <td><%= link_to "Enroll", {:controller => "course_users", :action => "enroll", :courseid => course.id, :userid => current_user.id} %></td>
    </tr>
  <% end %>

</table>


<br>
<br>
<br>
<br>
<%= link_to 'Regresar', course_users_index_path %>
